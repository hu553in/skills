interface:
  display_name: 'Staged code review'
  short_description: 'Iterative staged-change review'
  default_prompt:
    'Use $staged-code-review to review my staged changes within the requested scope, fix actionable
    issues when authorized, preserve my staged index, and leave new fixes unstaged.'
